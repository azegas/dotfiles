;;; scss-mode.el --- scss mode for emacs
;;; Commentary:
;; -
;;; Code:

(use-package scss-mode
  :ensure t
  ;; :config
  ;; ;; activate it for all buffers
  ;; (setq-default save-place t)
  ;; (save-place-mode 1)
  )

;;; scss-mode.el ends here
